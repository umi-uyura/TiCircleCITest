machine:
  timezone: Asia/Tokyo
  environment:
    XCODE_SCHEME: TiCircleCITest
    XCODE_PROJECT: TiCircleCITest.xcodeproj

dependencies:
  pre:
    - brew install n
    - n 4.4.5
    - npm install -g titanium tisdk
    - tisdk install 5.3.0.GA

test:
  override:
    - echo Hello World
    - which node
    - which ti
