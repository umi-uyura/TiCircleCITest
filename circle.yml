machine:
  timezone: Asia/Tokyo

dependencies:
  pre:
    - brew install n
    - n 4.4.5
    - npm install -g titanium
    - ti sdk install 5.3.0.GA

test:
  override:
    - echo Hello World
    - which node
    - which ti
